import Layout from "@/ui/organisms/Layout/Layout";

const Blog: React.FC = (): React.ReactElement => {
  return (
    <Layout>
      <h1>Blog Page is active</h1>
    </Layout>
  );
};
export default Blog;
